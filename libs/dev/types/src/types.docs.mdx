---
labels: ['watheia', 'compendium', 'dev', 'types']
description: 'Compendium helper types'
---

Utility types used in react development.

## CSSModule

```ts
export interface CSSModule {
  readonly [key: string]: string;
}
```

## SVGComponent

```ts
export type SVGComponent = React.FunctionComponent<
  React.SVGProps<SVGSVGElement> & { title?: string }
>;
```

## MDXComponent

```ts
export type MDXComponent = (props) => JSX.Element;
```

## Maybe

```ts
/**
 * Utility type representing optional data, which is
 * represented as a null value in json
 */
export type Maybe<T> = T | null;

/**
 * Utility type to create a new type from an existing
 * one (T), given a list of keys to make optional (K)
 */
export type MakeMaybe<T, K extends keyof T> = Omit<T, K> & {
  [SubKey in K]: Maybe<T[SubKey]>;
};
```
